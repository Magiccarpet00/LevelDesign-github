using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestMainCollision : MonoBehaviour
{

    public GameObject[] noeud; 

    void Start()
    {
        /*
        for (int i = 0; i < noeud.Length; i++)
        {
            Noeud n = noeud[i].GetComponent<Noeud>();

            n.regarderCote();
        }
        

        Noeud n = noeud[0].GetComponent<Noeud>();
        n.regarderTuilleVoisine();
        */
    }

    
}
